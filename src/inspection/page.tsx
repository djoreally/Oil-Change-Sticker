import React from "react";
import InspectionForm from "./InspectionForm"; // Adjust the path if necessary

const InspectionPage: React.FC = () => {
  return (
    <div>
      <h1>Inspection Form</h1>
      <InspectionForm />
    </div>
  );
};

export default InspectionPage;
